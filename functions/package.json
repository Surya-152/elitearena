{
  "name": "elitearena-functions",
  "description": "Firebase Cloud Functions for EliteArena esports platform",
  "version": "2.0.0",
  "private": true,
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build":    "tsc",
    "build:watch": "tsc --watch",
    "serve":    "npm run build && firebase emulators:start --only functions",
    "shell":    "npm run build && firebase functions:shell",
    "start":    "npm run shell",
    "deploy":   "firebase deploy --only functions",
    "logs":     "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin":    "^12.1.0",
    "firebase-functions": "^4.7.0"
  },
  "devDependencies": {
    "typescript":        "^5.4.5",
    "@types/node":       "^20.12.12"
  }
}
